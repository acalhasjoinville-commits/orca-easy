import { SuperAdminAparencia } from "@/components/super-admin/SuperAdminAparencia";

// Compatibility wrapper kept temporarily so older references do not break.
export function SuperAdminConfiguracoes() {
  return <SuperAdminAparencia />;
}
